
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public write access for registrations
    match /registrations/{registrationId} {
      allow create: if true;
      allow read: if true; 
    }
  }
}
